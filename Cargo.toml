[package]
name = "cpu57"
version = "1.0.0"
edition = "2021"
authors = ["CPScript"]

[dependencies]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = 3
lto = true
codegen-units = 1

[build-dependencies]